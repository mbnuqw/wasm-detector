{
  "name": "wasm-detector",
  "version": "1.0.0",
  "description": "WebExtension for detecting WebAssembly on the web-page.",
  "scripts": {
    "dev": "./node_modules/.bin/web-ext run",
    "build":
      "./node_modules/.bin/web-ext build -a dist/ -i *.sublime-* *.lock ./package.json",
    "lint": "./node_modules/.bin/web-ext lint"
  },
  "main": "background/main.js",
  "author": "<maxbadryzlov@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "web-ext": "^2.0.0"
  }
}
